spring:
  flyway:
    enabled: true
    locations: classpath:db/migration

#  #datasource
  datasource:
#    url: jdbc:postgresql:///business_owner_test_db
    #driver-class-name: org.postgresql.Driver
#    username: postgres
#    password: postgres

#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: validate

  config:
    activate:
      on-profile: test
