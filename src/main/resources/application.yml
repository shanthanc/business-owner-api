#Flyway
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

#Context-path
server:
  servlet:
    context-path: /business-owner/service